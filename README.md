# DevStore

#### 介绍

DevStore 是 DevStation 平台默认的软件商店，面向开发者提供 MCP 服务、OEDP 插件的快速安装能力。


#### 首次使用

如果您首次打开 DevStore，界面上没有任何组件，则需要手动启动 dev-store 的服务。

```bash
sudo systemctl start dev-store && sudo systemctl enable dev-store
```
